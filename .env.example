# ─── Geocoding (server-side, Nominatim) ───────────────────────────────────────
# No API key required — Nominatim is free and open
NOMINATIM_USER_AGENT=doctor-mapper/1.0
GEOCODING_CACHE_PATH=./geocoding-cache.json
